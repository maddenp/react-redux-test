import React from 'react';
import TestUtils from 'react-addons-test-utils';
import { expect } from 'chai';

import ListItem from '../src/components/ListItem';

describe('ListItem', () => {
  it('should pass this trivial test', () => {
    expect(true).to.equal(true);
  });
});
